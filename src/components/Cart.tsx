import React from "react";

import { Container } from "react-bootstrap";

export const Cart = (): JSX.Element => {
    return (
        <Container style={{ height: "92%", backgroundColor: "#e6e6e6" }}>
            Cart
        </Container>
    );
};
