import React from "react";

import { Container } from "react-bootstrap";

export const AdminInventory = (): JSX.Element => {
    return (
        <Container>
            <h2>Inventory</h2>
        </Container>
    );
};
