import React from "react";

import { Container } from "react-bootstrap";

export const AdminUsers = (): JSX.Element => {
    return (
        <Container>
            <h2>Users</h2>
        </Container>
    );
};
