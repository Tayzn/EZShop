import React from "react";

import { Container } from "react-bootstrap";

export const AdminOrders = (): JSX.Element => {
    return (
        <Container>
            <h2>Orders</h2>
        </Container>
    );
};
